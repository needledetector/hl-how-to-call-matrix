# このリポジトリで作業するエージェントへ

## プロジェクトの前提

- 特定の Google スプレッドシートを表示する「呼称マトリクス」専用の静的 PWA。汎用ライブラリや配布用テンプレートではない。
- 利用者向け README は作らず、開発に必要な情報・判断・検証手順はこのファイルに残す。
- フレームワーク・ビルド工程・npm 依存はない。この構成を踏まえて変更する。
- PC とスマホの両方で使う。見た目だけでなく、タップ数、キーボード表示時の操作、描画負荷を確認する。

## コードの所在

| ファイル | 責務 |
| --- | --- |
| `index.html` / `style.css` | 画面構造、レスポンシブ表示、配色 |
| `app.js` | 描画、操作、フィルター用 CSS、URL 状態の保存・復元 |
| `data.mjs` | シート取得、CSV・呼称記号の解析、データ組み立て、前回データの保存 |
| `search.mjs` | 日本語検索の正規化、呼称・セル・行列の判定 |
| `sw.js` | アプリ本体とシートのキャッシュ |
| `convert.py` | Excel から予備の `data.json` を生成 |

接続先のシート ID とシート名は `data.mjs` にある。「呼称表」「補助データ」「軸マッピング」の3シートを使う。`data.json` はリポジトリに同梱されていないため、フォールバック先として存在する前提にしない。

## 維持する仕様・利用者の好み

- 「呼ぶ人」と「呼ばれる人」は独立して選択し、方向を入れ替えられる。共通の人物範囲はグループ・期生・人物で絞れる。
- スマホ幅（640px以下）の初期表示は一覧、それ以外は表。一覧は一致する記録を40組ずつ表示し、未調査の組み合わせは表から確認できる。
- 絞り込みはタップごとに「指定なし → 限定 → 除外」。状態をチップ内にも表示する。利用者は絞り込みのドロップダウンを操作が面倒だと感じている。
- 表示設定は絞り込みと分離し、選択肢はラジオボタンで直接選べるようにする。
- スマホの検索欄は画面下にフローティング表示する。キーボード、セーフエリア、結果一覧やページ送りとの重なりに注意する。
- 「詳細…」は呼称より目立たない薄いグレー。タップ可能な領域とキーボードフォーカスは維持する。省略されたセルには「続きを読む」を表示する。
- 初期設定は空の行・列を隠し、見出しはフルネーム。URLに明示された設定があれば、初期値より優先して復元する。
- 表の倍率は60〜160%。標準の100%へ戻せる。倍率・人物・検索語・絞り込みなどはURLに保存する。
- URLには初期値から変更した項目だけを保存し、すべて初期値ならハッシュも付けない。従来の全設定入りURLも復元する。表／一覧の比較基準は起動時の画面幅で決め、画面回転で変えない。表示方法の指定がない共有URLは開く端末の初期表示を使う。
- タイトル右に小さく薄いバージョン表示を置く。`index.html` の表示番号・読み上げラベルは `sw.js` のキャッシュ版番号と揃えて更新する。
- 初期配色はライト。表示設定のボタンでライト／ダークを切り替え、配色はブラウザーに保存する。

## 検索・絞り込みの意味

- ひらがなを含む検索はカタカナにも一致する。カタカナのみの検索では、ひらがなと区別する。英字の大小・全角半角・空白は正規化する。
- 呼称フラグと軸の「限定」は OR 条件。「除外」を優先する。
- セル状態の除外は状態の注記を対象とする。同じセルに呼称があれば、呼称側の条件でも判定する。
- 空行・空列の非表示は、選択中の人物間で条件に一致するセルがある行・列をそれぞれ残す。
- 検索件数は人物範囲と絞り込みに一致する呼称だけを数える。一致セルへの移動は、一覧のページ切替にも対応する。

## 性能上の注意

- 実データは確認時点で90人、8,100セル規模。小さなテストデータだけでは描画の重さを判断できない。
- 過去に「続きを読む」の判定で全セルの寸法を毎回読み、リンクを書き換えて大きく遅くなった。全セルの同期レイアウト計測を復活させない。
- 寸法判定は IntersectionObserver で追跡する画面内のセルに限定する。読み取りをまとめ、表示が変わる場合だけ書き換える。
- 検索条件が同じ場合は判定結果を再利用する。データ更新時には結果のキャッシュを無効化する。
- 配色切替で表を作り直さない。略称／フルネーム切替では見出しだけを更新し、セルとスクロール位置を維持する。

## ローカル確認・テスト

- `python -m http.server 8000` で配信し、`http://localhost:8000` を開く。ES Modules を使うので `file://` では確認しない。
- 本番は HTTPS と、`.mjs` を JavaScript の MIME タイプで配信できる環境が必要。
- Node.js 24 で `node --test tests/*.test.mjs`。追加パッケージは不要。解析、検索、状態復元、キャッシュなどを検証する。
- `node tests/browser.mjs` はテスト用シートを使う Chrome / Edge のヘッドレス UI テスト。スマホ縦横・PC幅、人物選択、検索件数、ページ送り、URL復元、配色、詳細画面などを確認する。
- ブラウザーは Windows の標準インストール先から検出する。それ以外は実行ファイルのパスを引数または `CHROME_PATH` で渡す。スクリーンショットは一時ディレクトリに保存される。
- `node tests/browser.mjs --live --offline` は実シートを取得した後、通信失敗時の前回データへの切替を確認する。ページの通信遮断に加え、シートの fetch 失敗を明示的に再現する。公開環境は `--url=https://.../` で指定できる。
- `node tests/browser.mjs --perf` は実データで検索・解除を6回行い、CPUを4倍遅くしたChromeで応答時間・レイアウト時間・寸法読み取り回数を測る。ネットワーク取得時間は含まない。
- `--perf --revision=コミットハッシュ` で作業ファイルを変更せず過去の版を測定できる。初期設定が変わっている場合は、表示条件も揃えて比較する。
- 通常のUIテストは実シートにアクセスしない。キャッシュの単体テストもブラウザーAPIの代替実装を使う。実データ、PWAの更新、iOS Safari実機を検証したことにはならない。実施した検証と未確認の範囲を区別する。

## 更新・キャッシュ

- 配信するアプリ本体を変えたら `sw.js` の `CACHE` の版番号を上げ、新しい必須ファイルを `ASSETS` に追加する。ドキュメントだけの変更では不要。
- 必須ファイルをすべて取得できた場合だけ、新しい Service Worker を有効にする。版の異なる HTML と JavaScript が混在する更新に注意する。
- シートはネットワーク優先。初回の成功時からデータ全体を保存し、通信失敗時は前回データであることを表示して使う。
- データ保存領域はアプリ本体のキャッシュ更新時にも保持する。初回利用や保存データ削除後はオンライン取得が必要。
- 公開サイトの読み込み不具合では、新規ブラウザーでの成功だけで解決としない。既存キャッシュ、再読み込み、実際のエラー文も確認する。
